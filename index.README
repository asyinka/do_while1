A simple code for user to input name 
onclick, the label welcomes user.

The next code takes two inputs and then automatically calculate hypotenuse 

The next is a code to increase, reset and decrease number, onclick

Next is a code to execute a dice game